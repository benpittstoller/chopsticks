{
  "name": "server",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "build": "esbuild server.ts --bundle --outfile=server.js --platform=node",
    "dev": "set NODE_ENV=development&&nodemon server.ts",
    "start": "set NODE_ENV=production&&node server.js"
  },
  "dependencies": {
    "@types/express": "^4.17.21",
    "express": "^4.19.2",
    "nodemon": "^3.1.0",
    "socket.io": "^4.7.5",
    "ts-node": "^10.9.2"
  },
  "devDependencies": {
    "esbuild": "0.20.2"
  }
}
